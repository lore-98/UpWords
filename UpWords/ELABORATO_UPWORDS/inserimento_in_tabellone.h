//HEADER CHE CONTIENE LE FUNZIONI PER L'INSERIMENTO DELLE PAROLE NEL TABELLONE//
#ifndef INSERIMENTO_IN_TABELLONE_H
#define INSERIMENTO_IN_TABELLONE_H
#include <iostream>
#include <string>
#include <vector>
#include "griglia.h"

using namespace std;
/*
void controllo_spazi_liberi(int coordx, int coordy, string word, char direzione)
{
    int y=coordy-1;
    int x=coordx-1;

    if(direzione=='v')
    {
        for(unsigned int riga=x;riga<x+word.length();riga++)
        {
            if(tabellone[riga][y].indice==1)
                return cellavuota
            else
                return cellapiena
        }
    }

    if(direzione=='o')
    {
        for(unsigned int col=y;col<y+word.length();col++)
        {
            if(tabellone[x][col].indice==1)
                return cellavuota
            else
                return cellapiena
        }
    }
}
*/

#endif // INSERIMENTO_IN_TABELLONE_H
